//quad
int main() {
	long double a, b, c, d;
	cin >> a >> b >> c;
	if (((fabs(a) < (1.7e-154)) and (a!=0)) or ((fabs(b) < (1.7e-154)) and (b!=0)) or ((fabs(c) < (1.7e-154)) and (c!=0)) ) {
		cout << "vvedeni slishkom malen'kie znacheniya";
	}
	if ((fabs(a) > (1.7e154)) or (fabs(b) > (1.7e154)) or (fabs(c) > (1.7e154))) {
		cout << "vvedeni slishkom bol'shie znacheniya";
	}
	
	else if ((a == 0) and (b == 0) and (c == 0)) {
		cout << "x - luboe";
	}
	else if ((a == 0) and (b != 0)) {
		cout << ((-c) / b);
	}
	else if ((a==0) and (b==0)){
		cout << "net resheni1";
	}
	else if ((a != 0) and (b == 0)) {
		if (((-c) / a) >= 0) {
			cout << sqrt((-c) / a);
		}
		else {
			cout << "net resheni2";
		}	
	}
	else {
		
		d = b * b - 4 * a * c;
		if (d < 0) {
			cout << "net resheni3";
		}
		if (d == 0) {
			cout << (-b) / (2 * a);
		}
		if (d > 0) {
			cout << (-b + sqrt(d)) / (2 * a) << ' ' << (-b - sqrt(d)) / (2 * a);
		}
	}
	return 0;
}
