//calc-n
int main(){
	int a, b;
	cin >> a >> b;
	cout << a + b << endl << a - b << endl << a * b << endl << a / b; 
	return 0;
}

// calc-f
int main() {
	double a, b;
	cin >> a >> b;
	
	cout.precision(16);
	cout << a + b << endl << a - b << endl << a * b << endl;
	cout << fixed;

	cout << a / b << endl;
	return 0;
}
